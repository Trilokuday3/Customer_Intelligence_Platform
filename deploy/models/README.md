Model files baked into the API image. They must come from the same
`scripts/build_backend_data.py` run that filled the database's predictions;
re-seeding the database means copying fresh files from `models/` here.
